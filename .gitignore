__pycache__/
*.pyc
.env
*.pem
# Per-developer cloud infra (account-specific config, instance ids, keys).
# Shared out-of-band, not committed — same convention as the source harness.
aws/.instance_id
aws/*.pem
